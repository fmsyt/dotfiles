set -x VIM_MODE cli
#alias vim nvim

# set -gx XDEBUG_CONFIG "idekey=motsuni client_host=127.0.0.1 client_port=9001"

# keychain
if status --is-interactive
    if command -v keychain &>/dev/null
        set -x SSH_AUTH_SOCK $SSH_AUTH_SOCK
    end
end

# vim: set ft=fish ts=4 sw=4 sts=4 et:
